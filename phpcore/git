uzitecne git prikazy:

oprava cache kvuli .gitignore:

git rm -r --cached .
git add .
git commit -m 'oprava cache .gitignore'

smazani posledniho commitu
git reset --soft HEAD~1
nebo tvrde:
git reset --hard HEAD~1
